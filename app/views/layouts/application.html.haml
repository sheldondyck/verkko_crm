!!!
%html
  %head
    %title
      = title
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = csrf_meta_tags
    -# put google analytics js and all thirdparty js at end of body
  %body
    = yield
= debug(params) if Rails.env.development?
-#= debug(Rails.env)
